<section class="dashboard">
<h2>Hi {{ user.username }}, Welcome to your Dashboard</h2>

{{#each posts}}
  {{#if (isEqual user.username this.author)}}
    {{> post}}
  {{/if}}
{{/each}}

<button id="new-post-button">+ New Post</button>
</section>
